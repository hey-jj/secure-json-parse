# Changelog

## [0.1.1] - 2026-07-07

### Fixed

- `scan` with `Action::Remove` now handles deeply nested removed `__proto__` and `constructor.prototype` subtrees without overflowing the stack. (#11)

## [0.1.1] - 2026-07-07

### Fixed

- `scan` with `Action::Remove` now handles deeply nested removed `__proto__` and `constructor.prototype` subtrees without overflowing the stack. (#11)
